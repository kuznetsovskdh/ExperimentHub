[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
markers =
    slow: длительные симуляции (тысячи прогонов)
    live: требует поднятого HTTP-сервиса и БД
